$(function () {
  const swiper = new Swiper(".swiper", {
    // loop: true,
    // effect: 'fade',
    // speed: 600,
    pagination: {
      el: ".swiper-pagination",
      clickable: true,
    },
  });
});
